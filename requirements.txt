# core (Phase 1, CPU)
scikit-learn>=1.1
pandas
numpy
scipy
# data + eval
datasets>=2.18
huggingface_hub
# optional layers (P2/P3 - embeddings, guard models; GPU recommended)
sentence-transformers
faiss-cpu
transformers>=4.44
torch
# tracking / red-team (P5)
wandb
